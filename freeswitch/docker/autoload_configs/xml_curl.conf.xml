<!--
  mod_xml_curl Configuration (Docker Container)
  
  Fetches dynamic directory and extension authentication from the Node.js app container.
-->
<configuration name="xml_curl.conf" description="cURL XML Gateway">
  <bindings>
    <binding name="krad-directory">
      <param name="gateway-url" value="http://app:3000/fs-config"/>
      <param name="bindings" value="directory"/>
      <param name="timeout" value="5"/>
      <param name="connect-timeout" value="3"/>
      <param name="disable-100-continue" value="true"/>
    </binding>
  </bindings>
</configuration>
